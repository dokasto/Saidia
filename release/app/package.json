{
  "name": "Saidia",
  "version": "0.0.1",
  "description": "AI Teacher's Assistants",
  "main": "./dist/main/main.js",
  "scripts": {
    "rebuild": "electron-rebuild --parallel --types prod,dev,optional --module-dir . --version 37.2.0",
    "postinstall": "npm run rebuild && npm run link-modules",
    "link-modules": "node -r ts-node/register ../../.erb/scripts/link-modules.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "ollama": "^0.5.16",
    "sequelize": "^6.37.7",
    "sqlite-vec": "^0.1.7-alpha.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-rebuild": "^3.2.9"
  }
}
